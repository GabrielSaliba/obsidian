## <span style="color:rgb(0, 176, 80)">Recommended Sequence</span>
 1. Restore GitHub Actions capacity.
 2. Create the Sentry source-map upload token.
 3. Save it in the approved password manager.
 4. Add SENTRY_AUTH_TOKEN to GitHub Staging.
 5. Add VERCEL_TOKEN to GitHub Staging for RPRT-91.
 6. Verify Vercel Preview branch dev has:
- APP_ENVIRONMENT=staging
- NEXT_PUBLIC_SENTRY_DSN
- No SENTRY_AUTH_TOKEN
 7. Prepare the temporary protected staging proof route.
 8. Open and merge the proof PR into dev.
 9. Verify the GitHub run, Vercel deployment, Sentry release, and source maps.
10. Trigger the exception once.
11. Verify:
- Environment is staging.
- Release equals the complete deployed commit SHA.
- Stack frames map to the TypeScript source.
- The message is sanitized.
- No request data, user data, cookies, headers, or tokens are present.
12. Remove the proof route in a second PR and deploy again.
13. Inspect GitHub, Vercel, artifacts, logs, and browser output for token exposure.
14. Add secret-free evidence to RPRT-70, RPRT-91, the Secrets inventory, and RPRT-5.
15. Complete RPRT-70.



## <span style="color:rgb(0, 176, 240)">First Operator Step</span>
In Sentry, create a CI token for organization studio-repertorio and project repertorio:
- Give it only the permissions required to create releases, associate commits, and upload source maps.
- Name it clearly, such as repertorio-staging-source-maps.
- Store the value directly in the password manager.
- Do not paste the value into chat, Linear, source files, shell history, or Vercel.
Then add it through GitHub Settings > Environments > Staging > Environment secrets as SENTRY_AUTH_TOKEN.
After this step, tell me only that the secret name exists. I can verify its metadata without reading its value.


------------------------------------------------------------

- APP_ENVIRONMENT: the workflow sets it.
- NEXT_PUBLIC_SUPABASE_URL: public Vercel configuration.
- NEXT_PUBLIC_SUPABASE_PUBLISHABLE_KEY: public Vercel configuration.
- SUPABASE_PROJECT_REF: non-secret configuration.
- CRON_SECRET: optional until a scheduled route is deployed.
- Production application secrets: intentionally deferred until the production Supabase project is approved.
The repository variables VERCEL_ORG_ID and VERCEL_PROJECT_ID are already configured correctly.